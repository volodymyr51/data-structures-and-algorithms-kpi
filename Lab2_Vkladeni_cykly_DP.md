# Лабораторна робота № 2
## Алгоритми з вкладеними циклами та метод динамічного програмування

**Дисципліна:** Структури даних та алгоритми (СДА)  
**Мова програмування:** С

> Загальна інформація про курс (перелік робіт, система оцінювання, середовище розробки) — див. SDA_Course_Info.md / .docx.

---

## 1. Мета лабораторної роботи

Засвоєння теоретичного матеріалу та набуття практичних навичок використання різних циклічних керуючих конструкцій, вкладених циклів, методу динамічного програмування та обчислення кількості операцій алгоритмів.

## 2. Постановка задачі

1. Задане натуральне число n. Обчислити значення заданої за варіантом формули (ряду або добутку, що наближено обчислює значення певної функції у заданій точці; точність наближення залежить від кількості ітерацій n).
2. Для розв'язання задачі написати **дві програми**:
   1. перша програма обчислює формулу за допомогою **вкладених циклів**;
   2. друга програма обчислює ту саму формулу за допомогою **одного циклу** з використанням **методу динамічного програмування** (тобто накопичуючи черговий доданок/множник на основі попереднього, без повторного обчислення статечних, факторіальних і подібних виразів «з нуля» на кожній ітерації).
3. Виконати розрахунок кількості операцій для кожного з алгоритмів за методикою, викладеною на лекції, додавши до неї підрахунок кількості викликів стандартних функцій.
4. Програма має правильно розв'язувати задачу при будь-якому заданому n, для якого результат обчислення коректно представляється типом `double`.
5. Результат виводити у форматі із **сімома знаками після коми**.

### 2.1 Загальні вимоги до побудови алгоритму (обов'язково прочитати)

У алгоритмі за 1-ю програмою має бути **гніздо двох циклів**. Якщо у формулі явно немає такого вкладення операцій, гніздо циклів отримують, удосконалюючи (розписуючи) обчислення складної операції. Наприклад:

- квадрат числа: 1 + 3 + 5 + ... + (2n–3) + (2n–1) = n²
- добуток: 2 + 4 + 6 + ... + (2n–2) + 2n = n(n+1)
- множення на коефіцієнт: 2 + 2 + ... + 2 = 2k
- степінь: 2 · 2 · ... · 2 = 2^k

Деякі функції, що часто зустрічаються у варіантах:

- Факторіал: n! = 1·2·3·...·n, 0! = 1.
- Біноміальний коефіцієнт: C(n,m) = n! / (m!(n−m)!)
- Сума: Σ(i=1..n) 2^i = 2 + 4 + 8 + ... + 2ⁿ
- Добуток: Π(i=1..n) i = 1·2·3·...·n

Суть лабораторної роботи — дослідити метод динамічного програмування на обчисленні формули. Формула за варіантом наближено обчислює значення певної функції в конкретній точці; точність наближення залежить від кількості ітерацій n. **Тому в звіті обов'язково навести графік похибки обчислень у залежності від n.**

У формулах варіантів апроксимуються різні функції та константи:

- якщо це функція від x — для обчислення приймається x, вказаний при формулі (2, 3, 1/2, 1/3 або 1, залежно від варіанта);
- якщо сума чи добуток мають межу підсумовування до ∞ — вона приймається рівною n.

## 3. Вимоги до оформлення звіту

Обов'язково в протоколі має бути (**за відсутність нижчепереліченого знімаються бали**):

1. Титульна сторінка.
2. Завдання на лабораторну роботу + умова завдання за варіантом.
3. Текст обох програм (програмний код).
4. Скріншоти тестування програм (щонайменше 2 для n ≥ 2).
5. Таблиця з результатами запуску програм (значення лічильника операцій) для різних значень n = 1, 2, 3, 10, 20, 30, 50, 100.
6. Графік за таблицею (залежність кількості операцій від n) для обох способів.
7. Результати перевірки правильності обчислень на калькуляторі (щонайменше 2 для n ≥ 2).
8. Висновки за результатами лабораторної роботи.

Діаграми алгоритмів та письмові відповіді на контрольні питання — **не обов'язкові** (за їх відсутність бали не знімаються).

### 3.1 Обчислення

Для обчислення цілочисельних степенів немає потреби застосовувати функцію `pow` — вона обчислюється через логарифм, що вимагає більше обчислень, ніж звичайне множення. Варто дбати про оптимізацію обчислень і застосовувати простіші операції.

Обчислення мають відбуватися з високою точністю: використовуйте `double`, а не `float` — `float` дає похибку округлення вже за кілька операцій (особливо при діленні й множенні), тоді як `double` має вбудований механізм більш точного округлення.

### 3.2 Перевірка правильності обчислень

Декілька результатів (з тих, що є на скріншотах) слід перевірити на калькуляторі: підставити вхідні дані у формулу й переконатися, що результат збігається. Калькулятор можна обрати будь-який, або порахувати вручну. У звіт вставити скриншот калькулятора або ланцюжок покрокового обчислення.

Зважайте, що обчислення мають відбуватись з високою точністю — якщо похибка виникає вже в другому розряді після коми (а то й лівіше), десь є помилка.

### 3.3 Таблиця результатів

У програмі має бути змінна-лічильник, значення якої збільшується щоразу, як виконується певна операція. Враховуйте **всі** арифметичні операції (додавання, віднімання, множення, ділення), виклики функцій з бібліотеки `math` для обчислення `sin`, `cos`, `log`, `sqrt` (кожен виклик — 1 операція), операції присвоєння та інкременту, порівняння, перехід на наступну ітерацію циклу тощо. Після виконання програма виводить на екран не лише результат обчислення, а й кількість операцій.

Звіт має містити таблицю такого зразка:

| n | 1 | 2 | 3 | 10 | 20 | 30 | 50 | 100 |
|---|---|---|---|---|---|---|---|---|
| Кількість операцій, спосіб 1 (вкладені цикли) | | | | | | | | |
| Кількість операцій, спосіб 2 (динамічне програмування) | | | | | | | | |

### 3.4 Графік

За таблицею будується графік залежності кількості операцій від n для кожного зі способів. Бажано (не обов'язково) об'єднати обидва графіки в один — для зручнішого порівняння.

**Зверніть увагу:** шкала графіка повинна мати фіксовану ціну поділки, щоб графік мав правильну форму. Наприклад, у Google Таблицях (sheets.google.com): правий клік по діаграмі → Стиль діаграми → Оформлення → Сітки та позначки → обрати потрібну вісь → задати крок.

Додатково, оскільки формула апроксимує функцію, окремо потрібен **графік похибки обчислень у залежності від n** (див. розділ 2.1).

## 4. Контрольні питання

1. Класифікація циклічних керуючих конструкцій алгоритмів та особливості їх використання.
2. Запис циклічних керуючих конструкцій алгоритмів діаграмами дій.
3. Запис циклічних керуючих конструкцій алгоритмів мовою програмування.
4. Методика спрощеного підрахунку кількості операцій алгоритмів.
5. Метод динамічного програмування: визначення та значимість при створенні алгоритмів.

## 5. Варіанти завдань

> **Примітка щодо нумерації.** Це — оновлений (актуальний) перелік варіантів для цієї роботи, наданий викладачем окремим файлом («Нові варіанти для виконання лабораторної роботи №2», замінює застарілі варіанти з друкованої методички SDA-1_Metodichka). У джерельному файлі позначку «27)» через друкарську помилку має два різних завдання поспіль (стор. 4–5 джерела), через що наскрізна нумерація зсувається. Нижче нумерація виправлена наскрізно від 1 до 36: варіанти **1–27 відповідають своїм номерам у джерелі без змін**, а всі варіанти, що в джерелі надруковані після другого «27)», зсунуті на **+1** (тобто друге «27)» джерела = варіант **28** нижче, «28)» джерела = варіант **29** і т.д., аж до «35)» джерела = варіант **36**).
>
> Формули нижче перенесені в текстовий вигляд (замість зображень джерела) вручну. Позначення: `Σ(i=a..b) [...]` — сума за i від a до b; `Π(i=a..b) [...]` — добуток за i від a до b; `C(n,i)` — біноміальний коефіцієнт n!/(i!(n−i)!). Два записи потребують окремого зауваження щодо друку в джерелі — позначено виносками нижче (варіанти 15 та 18).

1. S = sin x = Σ(i=0..n) [(−1)^i · x^(2i+1)] / (2i+1)!  (x = 0.5)
2. P = cos x = 0.5 · Π(i=−n..n) { [2x − Σ(j=1..i) π] / [x + Σ(j=1..i) π] }
3. S = ln x = Σ(i=1..n) [(−1)^(i+1) · (x−1)^i] / i  (x = 1/3)
4. P = 2 = Π(i=2..n) [1 + 1/(2^i − 2)]
5. S = ln[(1+x)/x] = Σ(i=1..n) (−1)^(i+1) / (i·x^i)  (x = 5)
6. S = (1+x)^n = Σ(i=0..n) C(n,i) · x^i  (x = 0.2)
7. S = π/(2√3) = 1 + Σ(i=1..n) (−1)^i / [(2i+1)·3^i]
8. S = sinh²x = Σ(i=1..n) [2^(2i−1) · x^(2i)] / (2i)!  (x = 1/3)
9. S = erf(x) = (2/√π) · Σ(i=0..n) [(−1)^i · x^(2i+1)] / [i!·(2i+1)]  (x = 0.5)
10. P = cos x = Π(i=1..n) {1 − 4x² / [π²·Σ(j=1..i)(2j−1)]}  (x = 1/3)
11. S = Si(x) = Σ(i=0..n) [(−1)^i · x^(2i+1)] / [(2i+1)·(2i+1)!]  (x = 1/3)
12. P = sin x = (2x/π) · Π(i=1..n) ( 4·[x² − π²·Σ(j=1..i)(2j−1)] / {π²·[1 − 4·Σ(j=1..i)(2j−1)]} )  (x = 0.5)
13. S = ln(1+x) = Σ(i=1..n) [(−1)^(i+1) · x^i] / i  (x = 2.0)
14. P = sin(x)/x = Π(i=0..n) {1 − x² / [π²·Σ(j=1..i)(2j−1)]}  (x = 1/3)
15. S = 1/(1+x) = Σ(i=0..n) (−1)^i · x^i  (x = 1/3) — *у джерелі між `(−1)^i` та `x^i` надруковано крапку з комою; за змістом (це ряд геометричної прогресії) це знак множення, тож тут відтворено як `(−1)^i · x^i`.*
16. P = sinh x = x · Π(i=1..n) {1 + x² / [π²·Σ(j=1..i)(2j−1)]}  (x = 0.5)
17. S = sin²x = Σ(i=1..n) [(−1)^(i+1) · 2^(2i−1) · x^(2i)] / (2i)!  (x = 0.5)
18. P = cos x − sin x = Π(i=1..n) {1 + [(−1)^i · 4x] / [(2i−1) · Σ(j=1..i) π]}  (x = 0.5) — *у джерелі верхня межа внутрішньої суми Σπ надрукована нечітко (не видно показника степеня над Σ, на відміну від аналогічних формул). Тут прийнято за аналогією з рештою варіантів, що це Σ(j=1..i); за потреби звірте з оригіналом.*
19. S = 1/(1+x²) = Σ(i=0..n) (−1)^i · x^(2i)  (x = 1/3)
20. P = −sin(3x)/cos x = 3 · Π(i=1..n) {1 + 8x² / [x² − π²·Σ(j=1..i)(2j−1)]}  (x = 1/3)
21. S = cos²x = 1 − Σ(i=1..n) [(−1)^(i+1) · 2^(2i−1) · x^(2i)] / (2i)!  (x = 1/3)
22. P = cos(πx/4) − sin(πx/4) = √2 · Π(i=1..n) {1 − (1+x)² / [4·Σ(j=1..i)(4j−1)]}  (x = 2)
23. S = e^(−x²) = Σ(i=0..n) [(−1)^i · x^(2i)] / i!  (x = 1.5)
24. P = sin x = Π(i=1..n) {1 − x² / [π²·Σ(j=1..i)(2j−1)]}  (x = 0.5)
25. S = arctan x = π/2 − Σ(i=0..n) (−1)^i / [(2i+1)·x^(2i+1)]  (x = 0.5)
26. P = cos²x = [(π+2x)²/4] · Π(i=1..n) {1 − (π+2x)² / [4π²·Σ(j=1..i)(2j−1)]}  (x = 1/3)
27. S = 1 − ln 2 = Σ(i=1..n) (−1)^(i+1) / [(2i−1)·i·(2i+1)]
28. S = ln x = Σ(i=1..n) (x−1)^(2i−1) / [(2i−1)·(x+1)^(2i−1)]  (x = 1/3)
29. P = tan x + cot x = (1/x) · Π(i=1..n) {1 + 4x² / [π²·Σ(j=1..i)(2j−1) − 4x²]}  (x = 1/3)
30. S = Ci(x) = γ + ln x + Σ(i=1..n) [(−1)^i · x^(2i)] / [2i·(2i)!], де γ = 0.577215665  (x = 2/3)
31. P = sin(3x)/sin x = −Π(i=−n..n) {1 − 4x² / [x² + 2xiπ + π²·Σ(j=1..i)(2j−1)]}  (x = 1/3)
32. S = cos x = Σ(i=0..n) [(−1)^i · x^(2i)] / (2i)!  (x = 1/3)
33. S = sinh x = Σ(i=0..n) x^(2i+1) / (2i+1)!  (x = 1/3)
34. S = J₀(x) = Σ(i=0..n) [(−1)^i · x^(2i)] / [2^(2i)·(i!)²]  (x = 1/3)
35. P = cos x = Π(i=1..n) {1 − 4x² / [π²·Σ(j=1..i)(4j−1)]}  (x = 1/3)
36. S = ln x = Σ(i=1..n) (x−1)^i / (i·x^i)  (x = 3)

*Можна побачити формули у [папці Lab2_variants](Lab2_variants).* 
